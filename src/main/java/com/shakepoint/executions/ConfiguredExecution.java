package com.shakepoint.executions;

import com.shakepoint.dto.ScheduledExecution;

//TODO evaluate if is needed
public class ConfiguredExecution implements Execution {

        @Override public void schedule(ScheduledExecution scheduledExecution) {

        }
}
